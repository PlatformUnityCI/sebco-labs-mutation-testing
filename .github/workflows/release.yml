name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: PlatformUnityCI/cross-platform-guard/.github/workflows/release-governance.yml@v1.2.4
    
